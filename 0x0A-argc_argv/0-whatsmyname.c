#include "main.h"
#include <stdio.h>

/**
  * main - This will print its name
  * @argc: For argument count
  * @argv: For argument vector
  *
  * Return: Always zero
  */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
