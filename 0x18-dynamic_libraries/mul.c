#include <stdio.h>
#include <stdlib.h>

/**
 * mul - This will multiply two integers
 * @k: The first integer
 * @m: The second integer
 * Return: return multiplied answer
 */

int mul(int k, int m)
{
	return (k * m);
}
