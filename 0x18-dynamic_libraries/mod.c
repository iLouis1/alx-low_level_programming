#include <stdio.h>
#include <stdlib.h>

/**
 * mod - This is the mod of two integers
 * @k: The first integer
 * @m: The second integer
 * Return: return the final answer
 */

int mod(int k, int m)
{
	return (k % m);
}
